package difficultyMedium;

import helperClass.TreeNode;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

public class TrimABinarySearchTreeTest {

    private TrimABinarySearchTree solution;
    private TreeNode root;
    private TreeNode resultRoot;

    @Before
    public void setUp() throws Exception {
        solution = new TrimABinarySearchTree();
    }

    @Test
    public void trimBST() {
        //root = new TreeNode(3, new TreeNode(0, null, new TreeNode(2, new TreeNode(1), null)), new TreeNode(4));

    }
}